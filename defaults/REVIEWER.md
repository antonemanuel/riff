# Reviewer Agent Instructions

Add project-specific instructions for the reviewer agent here.

Examples:
- Review focus areas (security, performance, correctness, style)
- Acceptance bar (what counts as APPROVED vs CHANGES_REQUESTED)
- Tests that must pass before approval
- Code style standards to enforce
- Patterns to watch for (e.g. missing error handling, hardcoded values)
